---
const { image, title, body, href = "#", reverse = false, imageShape = "circle" } = Astro.props;
const shapeClass = imageShape === "square" ? "img-square" : "img-circle";
---

<div class={`spot-row ${reverse ? "md:[&>div:first-child]:order-2" : ""}`}>
  <!-- Copy (≈ 2/3) -->
  <div class="spot-copy">
    <h3 class="text-xl md:text-2xl font-semibold mb-2">{title}</h3>
    <p class="text-muted mb-4">{body}</p>
    <a href={href} class="spot-link hover:bg-slate-50 focus:outline-none focus:ring-2 focus:ring-primary/30">
      Learn more
    </a>
  </div>

  <!-- Image (≈ 1/3) -->
  <div class="flex md:justify-start justify-center">
    <img src={image} alt="" class={`spot-img ${shapeClass}`} loading="lazy" />
  </div>
</div>
